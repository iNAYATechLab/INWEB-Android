<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is normally blocked on Android 9+. INWEB *is* the server,
  so we opt-in exactly for the addresses the user's own server might use:
  localhost + all private IPv4 ranges. Everything else stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Full RFC1918 private-network coverage -->
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
